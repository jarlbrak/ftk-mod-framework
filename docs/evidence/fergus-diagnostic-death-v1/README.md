# Fergus death-first diagnostic

[Validation](validation.json) preserves explicit KillSingle from58HP, followed by120raw frames and a verified120-frame MP4. Root reviewed30/60: the probe collapses into a visible heap with the retained native hat behind the hero. The exact root review, action journal, binary/profile/asset/session pins, capture summary and progression observations are archived. Video is12fps and pads odd dimensions without cropping.

`deathHeavy_imp` has95positive observed frames25–119; all120keep Animator and renderer enabled and m_DoRagdollfalse. The native source prefab has no Rigidbody. Runtime adds a separate active kinematic weapon body under WEAPON_HOLDER/scourgeG(Clone) at0–24;25–119record zero rigidbodies. These are animated-body death observations, not ragdoll acceptance.

Two once-only native Collect submissions reached strict Ready0/3, queued Trap1. A fresh observation between same-button submissions records gold18→34; the repeated button identity is not treated as evidence that one submission was skipped. The original progression files remain unchanged.

This is a separate diagnostic encounter on exact36-bone renderer121222/controller5934. It does not turn the earlier stopped pass/theft/flee case into a death or full sequence PASS. Normal hit, ordinary lethal damage, original Thistlewick body/hat fit and global Fergus haunt progression remain outside this evidence.
