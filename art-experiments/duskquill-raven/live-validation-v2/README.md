# Duskquill Raven fresh live trial V2

This supplement records the fresh catalog-411 run in session `594b337748914937b26473ee389ea71e` using the exact crowC chassis at public visual-scale factor `1.0` (captured native CEL scale `0.9`). The authored `duskquill.glb` bound to `enCrow` under one live enemy owner with the expected 36-joint bone signature.

Selected idle and attack views show the dark raven body, wings, head and pale beak staying connected through native flight motion. The runtime inventory reports the authored basecolor and the native Standard material `matCrow (Instance)` with its emission keyword present but black emission and no emission map; the native half-gray tint remains part of the live appearance.

Pass, ordinary attack and explicit `KillSingle` fixture captures are complete 120-frame recordings. The ordinary attack changes the same target from HP 58 to 53 with `cheat=None` and no focus. The explicit kill fixture reaches the native Victory transition and strict Ready at level 0 room 2. That encounter exposes a Ready vote directly, so no Collect action is claimed.

Root reviewed two idle frames, two attack frames and the kill endpoints. The small/dark airborne silhouette, native UI/effects and victory overlay limit fine feather/eye detail, full death deformation, settled-ragdoll, culling-envelope, portrait/resource lifetime and finished-art acceptance.

`validation.json` preserves the fresh case result, setup/action journals, helper responses, authoring assets and the prior V1 live record as gzip-lossless metadata, all current source-image hashes, six selected originals and presentation videos for all three complete captures. Native payloads and DLLs are excluded. `archive.py` is offline-only and refuses a completed destination.
