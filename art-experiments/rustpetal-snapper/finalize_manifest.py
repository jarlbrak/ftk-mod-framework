"""Freeze a reviewed original Rustpetal package; never deploy or modify earlier catalogs."""
from pathlib import Path
import json,hashlib,numpy as np
O=Path(__file__).resolve().parent;R=O.parent.parent;sha=lambda p:hashlib.sha256(p.read_bytes()).hexdigest();ev=lambda p:dict(path=str(p.relative_to(R)),sha256=sha(p),bytes=p.stat().st_size)
f=R/'scratch/plant-d-native-topology-analysis/findings.json';findings=json.loads(f.read_text());ref=np.load(R/'scratch/plant-d-native-topology-analysis/reference-121537/reference.npz');d=json.loads((O/'rustpetal.source.json').read_text());w=np.array(d['weights']);j=np.array(d['joints']);used={int(i)for i in j[w>0]};nativeused={int(i)for i in ref['joints'][ref['weights']>0]};assert used==nativeused and len(used)==30 and len(d['bone_names'])==43
for n in ['rustpetal.validation.json','reopened-validation.json']:
 v=json.loads((O/n).read_text());assert v['bones']==43 and v['max_bind_rest_error']<1e-5 and v['source_normal_agreement']['positive_fraction']==1
assert json.loads((O/'original-geometry-proof.json').read_text())['status']=='PASS'
for label in ['pass','hit','death','hit-mouth']:
 a=json.loads((O/f'native-{label}-pose-audit.json').read_text());assert a['frames']==120 and a['glbSha256']==sha(O/'rustpetal.glb')
proof=dict(paletteBones=43,weightedBones=30,softVertices=int(((w>0).sum(1)>1).sum()),positiveVerticesByBone={n:int(((j==i)&(w>0)).any(1).sum())for i,n in enumerate(d['bone_names'])},nativeUnusedRetained=[n for i,n in enumerate(d['bone_names'])if i not in used]);(O/'weight-proof.json').write_text(json.dumps(proof,indent=2)+'\n')
p=dict(version=1,profiles=[dict(key='ftkmf_modeltest_rustpetal',baseEnemy='plantD',displayName='Rustpetal Snapper',combatProfile=findings['profile']['combatProfile'],renderers=[dict(rendererPath='enJungleNibbler_C',glbFile='rustpetal.glb',textureFile='rustpetal_basecolor.png',disableNativeEmission=True)],minimumBaseHealth=64,visualScale=1.0)]);(O/'runtime-profile.json').write_text(json.dumps(p,indent=2)+'\n')

r=dict(model='Rustpetal Snapper',status='ORIGINAL_OFFLINE_PACKAGE_REVIEWED_LIVE_PENDING',nativeEnemy='plantD',rendererId=121537,nativeCelScale=findings['root']['localScale'],visualScaleFactor=1.0,sourceFindings=ev(f),assets=[ev(O/n)for n in ['rustpetal.glb','rustpetal_basecolor.png']],files=[ev(p)for p in sorted(O.rglob('*'))if p.is_file()and p.name!='manifest.json'],limits=['Initial bind candidate preserved; no geometry revision required after native D pose fitting.','Open inner funnel and outer sleeve have scoped surface-orientation checks; not a globally watertight mesh claim.','Pass/hit normalize Root_M; death retains renderer-local travel; head closeup uses per-frame framing.','Native1.7 root scale retained via visual factor1, emission opt-out requires live material readback.','Exact D only; no G inheritance. No live original appearance, portrait, ordinarylethal or final teardown acceptance.']);(O/'manifest.json').write_text(json.dumps(r,indent=2)+'\n');print(sha(O/'manifest.json'))
