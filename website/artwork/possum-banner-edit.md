# Possum banner typography edit

Created with the built-in image generation tool on 2026-09-25. This is a website-only
promotional illustration edit, not a new mod version or a native screenshot.
The source PNG is `possum-banner.png`; `src/data/library-art.json` pins its SHA-256.
`prepare-media.mjs` compresses it into the public WebP and records the output hash.

Edit target: the published Possum 1.0.0 banner, resized to the website's 1280px WebP.
Typography references: the published Paladin 1.2.0 and Thief 1.0.0 promotional banners,
as pinned in `src/data/library-art.json`. No native game capture was supplied.

## Final prompt

Use case: text-localization. Edit only Image 1, the Possum promotional banner. Images 2 and 3 are typography/layout references only, not edit targets. Preserve the original possum character, pose, forest landscape, lighting, palette, framing and 16:9 composition. Replace the Possum title with exact text 'POSSUM' in the same flat ivory uppercase Roman serif lettering as PALADIN and THIEF in references: consistent capital height, weight, restrained serifs, no bevel, no 3D outline. Match the title block placement and visual size to those reference banners. Under the title place a fine horizontal gold divider with a small simple gold diamond exactly centered within the divider, matching the references; its vertical position should align with the reference divider around 51% down the full image and its horizontal center with the title block around 28% of image width. Beneath it use exact subtitle 'A NEW COSMETIC RACE' in the same small widely tracked uppercase gold Roman serif style as their subtitles. Completely remove the 1.0.0 version badge, frame, and its extra divider lines, restoring natural forest scenery in that area. No version text anywhere. Change only the title, subtitle, divider emblem and removed badge area; preserve the rest of Image 1. Output the single edited Possum banner only.
