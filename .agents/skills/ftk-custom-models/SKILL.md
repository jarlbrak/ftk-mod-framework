---
name: ftk-custom-models
description: Author, export, integrate, and validate custom FTK character models on native skeletons.
---

# Custom FTK models

The workflow body predates the `.agents/` convention and remains at
`skills/ftk-custom-models/SKILL.md` because public model guides link to that stable path.
Read that entry point completely and follow it as part of this skill.

Read only the reference under `skills/ftk-custom-models/references/` that the current step needs.
The entry point's routing table says which one, and each is self-contained. Do not load them all.
Its table links resolve against `skills/ftk-custom-models/`, never against an adapter directory.

Apply the root and `tools/ai-model-pipeline/AGENTS.md` instructions. Machine-specific game paths,
capture setup, and private knowledge services come only from the optional local overlay.
