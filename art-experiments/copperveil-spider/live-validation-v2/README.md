# Copperveil Weaver fresh live trial V2

This supplement records the fresh catalog-411 run in session `115d691fdeec4e829088462912641932` using Spider B, renderer `enSpiderB` (121386), owner `369188`, and visual scale `1.0`. The authored eight-legged surface bound to the exact native renderer and stayed readable across the pass, ordinary attack and explicit fixture captures.

The pass and attack are complete 120-frame recordings. The ordinary attack changed the same target from HP 63 to 61 with `cheat=None` and no focus. The explicit `KillSingle` fixture also completed 120 frames, removed the enemy, accepted one native Collect and reached strict native Ready at level 0 room 2. The fixture death is not ordinary lethal damage.

Root reviewed two settled idle frames, two poison-attack frames and the fixture endpoints. Body, copper abdomen markings, amber eyes, articulated legs and antennae remain readable; native poison effects and the hero foreground limit fine deformation and material inspection. The terminal view is the native victory/item-choice surface, so it does not establish detailed corpse or floor-contact acceptance.

`validation.json` preserves the case result, setup/action journals, helper responses, profile/runtime/authoring manifests as gzip-lossless metadata, all 360 source-image hashes, six selected originals and three 120-frame presentation videos. Native payloads and DLLs are excluded. `archive.py` is offline-only and refuses a completed destination.
