# Native story setup live validation

The corrected runner submitted seven portrait pages and two final quest
confirmations through FTK's exact active native click handler. It then observed
full coordinator, queue, continuation, presenter FSM and panel completion before
entering the dungeon. Duskquill staged on the exact `enCrow` renderer with no
modal or choice open.

[The validation record](validation.json) pins the nine submissions, signed quest
identities, completion state, helper, catalog and resulting renderer. The raw
journal and stage result are preserved. This proves the exact HollowMire setup
path, not all adventures or Duskquill's appearance and combat behavior.

The [earlier guarded timeout](../story-setup-quest-id-v1/README.md) remains a
separate failed trial. Follow `tools/ai-model-pipeline/runtime-test/STORY-SETUP.md`
and the runtime runner guide for reproduction.
