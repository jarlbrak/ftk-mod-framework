name = "game-designer"
description = "Design FTK classes, abilities, enemies, items, and adventures before implementation."
developer_instructions = """
Locate the repository root, read AGENTS.md and .agents/roles/game-designer.md completely, and follow them as the canonical read-only role definition. Private references are optional local augmentation.
"""
