---
name: ftk-custom-models
description: Author, export, integrate, and validate custom FTK character models on native skeletons.
---

Read `.agents/skills/ftk-custom-models/SKILL.md` and follow it as the generic entry point.
It sends you to the workflow body at `skills/ftk-custom-models/SKILL.md`.

That body is tiered. Read it in full, then load only the one file under
`skills/ftk-custom-models/references/` that the current step needs; its routing table says which.
Those table links are relative to `skills/ftk-custom-models/`, not to this adapter's directory.
Do not read the whole references directory.

Translate generic actions to the available Claude Code tools without changing project policy.
