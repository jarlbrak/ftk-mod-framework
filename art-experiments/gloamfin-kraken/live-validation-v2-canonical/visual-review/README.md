# Gloamfin Kraken visual follow-up V1

This review preserves the first exact-route gameplay capture made specifically to close the production adapter's visual gates. Session `8c1d6e0c22874f95ac9553182e6c262b` bound `ftkmf_glb_gloamfin.glb` to exact `krakenHead / enkrakenhead / krakenHead / 121260` under one live owner and one applied resource lease.

The pass capture is complete at 120 frames and includes settled `krakenIdle`, native `AttackProf` and `Attack`, almost the full `krakenAttack` interval, and return to idle. The ordinary no-focus hero attack capture is complete at 120 frames, records HP 324 to 316, native `Damaged`, and recovery. The explicit `KillSingle` capture retains 91 frames through coherent native `krakenDisappear`; the renderer becomes inactive at sample 90 and is then destroyed. The fixture is visual death evidence only. The already verified production campaign remains the authority for ordinary non-cheat lethal death, enemy victory and natural teardown.

Twenty-two original frames were reviewed. The custom face, mouth, tongue and lateral limbs remain connected through idle, attack, impact and the visible death prefix. The renderer stays active, enabled and visible for every live sample and becomes inactive only at native cleanup. The fixed combat camera keeps the readable silhouette in view, although the raised attack reaches behind the top HUD and the hero and native effects obscure the lower center.

The review found one actual blocker: every Kraken turn in the initiative row uses a blank teal tile instead of a custom model portrait. `review.json` records that as a failed portrait gate. This evidence is therefore not canonical route acceptance yet. Preserve the raw captures and fix the portrait path before a narrow live portrait follow-up and final immutable archive.

The root-level runner record is `scratch/model-route-6a28ac3cf4523c24-kraken-visual-v1-run.json`. The raw case is `scratch/mirewarden-game/model-test-output/case-1b583efeaae040949ec2399bc40e9768/case-result.json`. Both complete captures and the accepted death prefix remain unchanged under their recorded SHA256 values.
