# Amberwake reduced-scale trial candidate

The exact reviewed GLB is unchanged. The isolated trial requests public uniform
scale0.25 and emission opt-out. The22.374-unit authored wing span would become
approximately5.594world units. This is a camera-fit hypothesis, not acceptance.
The profile preserves native dragonFrost stats, controller and frost weapon effects.
Both portraits, wing tips, horns, tail, hero visibility, wing culling and the flat
ivory lower jaw still require live review. Native-scale natural-arena behavior is
separate from this dungeon fixture.

Freeze from an unfrozen candidate with `python3 finalize_candidate.py`. The command
refuses an existing manifest; preserve reviewed generations instead of overwriting.
The final manifest pins editable Blender files, original assets, source, audit
scripts, selected pose evidence, prior history and this trial decision.

Stage from the repository root:

```sh
python3 art-experiments/amberwake-dragon/stage_runtime_profile.py \
  --game-root "$PWD/scratch/mirewarden-game" \
  --output "$PWD/scratch/runtime-profile-409-amberwake"
```

The script verifies the pinned408catalog, authoring manifest and deployed binaries,
then preserves all prior rows and319assets and adds this row plus two assets.
Existing output is refused; pass a fresh `--output` to repeat. It discovers the
repository through ancestors even from its staged copy, or accepts `--repo`.
It never deploys or builds binaries. No live acceptance is claimed by staging.
