# Exact PufferB diagnostic

Three native captures contain120 frames each. Root reviewed pass0/60, ordinary attack30 (normal8 damage,58 to50), and explicit KillSingle death60 (50 to0). Two native Collect submissions reached strict Ready0/3. Raw wrapper status remains needs_visual_review, with the separate root verdict retained.

The native renderer first disables at27 during BlowFish_DeathDirect. Its conditional DeathFade event is source-consistent, but the actual CEL fade field was not observed. No ordinary lethal damage, indirect death, final resource teardown or original Saffronspine appearance is established.

Renderer121388 belongs to enPufferFishB/CEL139647, with exact B inverse binds. The30 names and controller5999 match A, but binds, attachment targets and EncounterCam differ. Source findings are pinned separately; no A appearance acceptance is inherited. Historical case pins retain catalog400 and helper0c65, regardless of later deployments.

Lossless raw gzip, verified120-frame12fps videos, reviewed PNGs, full action journals, root review, startup evidence and progression are linked from validation.json. Video dimensions are padded to even pixels; original image hashes are preserved.
