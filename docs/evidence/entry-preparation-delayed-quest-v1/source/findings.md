# Amberwake pre-entry Visit→Dungeon progression boundary

The stopped run is a startup sequencing observation, not an Amberwake model result. Original journal case1093997986f847d7bcec9699dfa71dba ends at sequence109; preserve it unchanged.

At sequence108 the actual registered current quest is -1 / ftkmf_hollowmire_arrive. All movement/story/continuation quiet fields are true, but exact expected DungeonQuestDef and its destination are false. EntryPreparation.cs intentionally requires ftkmf_hollowmire_crypt / FloodedCrypt. Python prepare() therefore stops BEFORE entry-position. Exact journal helper request counts: story-state18, story-submit4 (Visit pages0/1/2 then final confirm), quiet-tutorials1, fortify-party1, entry-preparation-state1. Zero entry-position/discover, dungeon-entry or staging submissions.

The later root-owned read-only snapshot directly observes current quest -10 / ftkmf_hollowmire_crypt, registered reference/expected definition/destination true. Native check FSM is Check Story Quest Start Message, stored setup continuation42 Self wait1, and an actionable guarded portrait is open. No movement mutation was needed to produce this transition. We did not observe the exact intermediate native completion call and do not claim which scheduling delay caused the gap.

Source anchors: RealmBossAdventure.Author defines Visit Q1 (three start pages) then Dungeon Q2 (four start pages). StageBuilder.AddVisitQuest uses RealmCapital and no delivery. VisitQuestLogic.IsCompleteState accepts current hero hex equal to Visit destination, unless waiting respawn. GameEventManager.GetNextStoryQuest/_advanceStoryQuest creates/registers the next actual quest and starts its native loop; CheckStoryQuestStartMessage/ShowQuestStartMessage coordinates the native start message; CheckQuestStatusDone disables the check FSM and continues native continuations. Message coordinator completion is therefore not equivalent to completion of the whole native quest loop between Q1 and Q2.

Bounded continuation: preserve original stop, pin same session/root/catalog/assets/binaries/party and assert zero prior entry actions. Use existing audited story-state/story-submit outside combat/dungeon for the newly observed exact native Q2 pages and confirm, once each with current callback/identity guards. Then observe fresh entry-preparation-state until expected quest and existing movement/check/continuation gates produce positionEligible with ticket null. Only then execute the first ordinary prepare_entry sequence, its existing fresh verify, and adjacent entry→stage. Do not resend old Q1 pages, force quest callbacks, teleport as a workaround, or replay new-run. Any uncertain submission, unexpected quest/surface/owner, or bounded timeout stops.

Reusable future correction: a bounded pre-position readiness loop must include current native quest identity/check state alongside message completion, allowing only the source-known Q1→Q2 transition and existing guarded story operations; readiness still requires exact Q2. A larger fixed sleep or merely broadening the exact quest gate is not a fix. No implementation or game actions were performed for this audit.
