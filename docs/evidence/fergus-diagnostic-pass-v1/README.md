# Fergus36-bone diagnostic: pass-only stopped exercise

[Validation](validation.json) preserves the original stopped wrapper: `Target no longer alive before next action`. Its one pass capture completed120frames. The MP4 has120frames at12fps, with odd dimensions padded rather than cropped; four root-reviewed PNGs are0/40/80/119. Raw telemetry and wrapper JSON are lossless gzip, with their original hashes retained, alongside stage/exercise/pass journals, binary/catalog/asset/session pins and the later automatic Ready observation.

Exact selection is `ftkmf_modeltest_probe_scourgeg`, native base `scourgeG` / Fergus, renderer121222 at `enScourgeLeprechaun`,36bones and controller5934 `LeprechaunController`. The original probe replaces the skinned body; the active rigid native hat remains. The portrait is hat dominated. This does not establish global Fergus haunt behavior for a synthetic cloned row.

During the pass, hero gold falls18→0 and HP988→968; enemy HP58→0 and `alive:false`. Captured clips include `attackProf_leprechaun` at42–60; no observed clip is a death clip. Root saw green flee indicators and the later disappearance. These facts are consistent with native theft followed by flee/removal, but exact `m_DidFlee` flags were not captured. HP0 and disappearance are **not** classified as death.

The wrapper stopped before any hit, explicit death or loot action. The next read-only fixture reports strict Ready0/3 automatically, queued Trap1 with enemy substitution ineligible. No Collect was submitted and no alternate enemy slot was forced. Separate hit-first and kill-first encounters remain necessary. No completed diagnostic sequence, finished art, complete controller coverage or native global scourge acceptance is claimed.
